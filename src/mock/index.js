/**
 * @description 导入d2Mock模拟后台API数据部分
 * 取消下面的注释即可使用
 */
// import d2Mock from './d2-mock'

// const req = context => context.keys().map(context)
// const options = req(require.context('./api/', true, /\.js$/))
//   .filter(e => e.default)
//   .map(e => e.default)
// console.log(d2Mock)
// options.forEach(option => {
//   d2Mock.load(option)
// })
